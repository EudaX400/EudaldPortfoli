---
const { href, imageSrc, title, description, githubLink, status } = Astro.props;
---

<div class="flex lg:flex-row flex-col mt-7 bg-green-100 dark:bg-zinc-950 rounded-lg shadow-2xl">
  <a href={href} class="group relative block lg:mr-6 lg:w-4/12">
    <img
      src={imageSrc}
      alt="Project photo"
      class="lg:w-max w-auto h-auto transform transition-transform duration-300 group-hover:scale-110 rounded-lg"
    />
  </a>
  <div class="lg:w-8/12 w-auto px-3">
    <h4 class="mb-2 mt-2 text-center">{title}</h4>
    <p>{description}</p>
    <div class="flex justify-between mt-4">
      <a href={githubLink} class="text-blue-500"> Github </a>
      <p class="text-green-800">{status}</p>
    </div>
  </div>
</div>
